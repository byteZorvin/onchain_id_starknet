/// Blocked till we solve which path we should take on proxies
/// - original protocol uses a beacon proxies
/// - We can achieve similar behaviour by querying class_hash
/// registered in ImplementationAuthority and check against locally stored one
/// and have an update mechanism accordingly


